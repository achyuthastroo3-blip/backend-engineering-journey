Today i learn about File handeling by using methods like "r", "w" and "a" 

"r": it's a method of file handeling by this method we acess the file and it has two functions that are read(), readline() by the read function we access entire file and with the readline() function we acess the single line it has start and endpoint it starts from starts point and going to next one if its null its prints " " the structure like(1->2->3->null) for each readline it passes to another point if we want starts from specific index we use readline(0) so here we saying that starts from 0 index

"w": it's a method of a file handeling by this method we write a line in the file but it clears the data which are exisit in file so it delete tha previous data and inserts the present line 
wirte("New line")

"a": it's a method of a file handeling by this method we append a new line in file it could't disturb the file just it appends the new value we gave 
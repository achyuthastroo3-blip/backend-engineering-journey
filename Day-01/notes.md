OOP FOUNDATIONS: Classes, Objects, Self, __init__, Methods, Instance Variables, Inheritance 
NEW THINGS: object name and class name start with capital. Overriding it is used like same method name but Different behavior This is one of the foundations of polymorphism. print(f"{self.name} says Woof ") it is the syntax of using to attach the String to variable  


Problem 1: Problems in your to-do list
In this problem they want count of questions rating more than thousand, I solved it easily but I face one error on taking input of array just I toke the input (rating=input()) so I modified into list and by using map covert into int elements so after that all test cases were passed

Problem 2: Small factorials
In this problem they want factorials of a given numbers , I solved it easily by using math inbuild function “math.factorial()” 
